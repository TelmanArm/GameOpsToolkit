using UnityEngine;

namespace Core.UI
{
    public class PopupController : PresentController
    {
        public PopupController(BasePresenter[] basePresenters, Transform generatePosition) : base(basePresenters, generatePosition)
        {
            
        }
    }
}
